import { Box, Button } from '@chakra-ui/react';
import React from 'react';

const Navbar = () => {
    return (
        <Box>
            <Button></Button>
        </Box>
    );
}

export default Navbar;
